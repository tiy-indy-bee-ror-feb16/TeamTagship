<%= simple_form_for @ship do |f| %>
  <%= f.input :body, label: false, placeholder: "What you shippin' bout?" %>
  <%= f.association :user %>
  <%= f.button :submit, "New Ship", id: "btn-ship" %>
<% end %>
