<% if current_user.blocking!(@user) %>
  <li><%= link_to "Block Dat", block_user_path(user_id: @user.id), class: "btn btn-primary buttons"%></li>
<% else %>
  <li><%= link_to "Unblock Dat", unblock_user_path(user_id: @user.id), class: "btn btn-primary buttons"%></li>
<% end %>
