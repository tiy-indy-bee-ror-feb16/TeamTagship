</div>
  <% if current_user.follows?(@user) %>
    <%= link_to "Unfollow", unfollow_user_path(user_id: @user.id), class: "btn btn-default"%>
  <% else %>
    <%= link_to "Follow", follow_user_path(user_id: @user.id), class: "btn btn-default"%>
  <% end %>
